
<h3>Welcome</h3>
<% if user_signed_in? %>
  <% if @user.admin? %>
    <p><%= link_to 'Create User', new_user_path %></p>
    <p><%= link_to 'Users:', users_path %> <%= User.count %> registered</p>
  <% else %>
   <%= link_to 'Upload Your Painting', new_painting_path %>
   <%= render 'paintings/index' %>
  <% end %>
<% end %>