
<h3>Welcome</h3>
<% if user_signed_in? %>
  <% if @user.admin? %>
    <p><%= link_to 'Users:', users_path %> <%= User.count %> registered</p>
  <% else %>
   <%= link_to 'Upload Your Image', new_image_path %>
   <%= render 'images/index' %>
  <% end %>
<% end %>